/* what is DOM?

   - Created by the browser 
   - Object represention of the html page
   - provides a progrmmmatic accces to the html page
   - 
*/

// using queryselec to select a particular element

const para = document.querySelector('p');

// console.log(para);

const paras = document.querySelectorAll('p');

// console.log(paras);

// paras.forEach((para, index) => console.log(para, index));


const error = document.querySelector('.error');

console.log(error);


const error2 = document.querySelector('body > div.error')

console.log(error2);





























































